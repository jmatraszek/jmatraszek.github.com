extends: default.liquid
title: Blog
route: posts
---

<h1>{{ title }}</h1>

{% for post in posts %}

<div>
  {{ post.date | date: "%b %-d, %Y" }}
    »
  <span class='post-title'>
    <a href="{{ site.path }}{{ post.path }}">{{ post.title }}</a>
  </span>
</div>

{% endfor %}
